- content_for :title do
  ! #{t('articles.images.add')} #{@article.headword_presentation}

- content_for :header do
  <em>#{t('articles.images.add')}</em> – #{link_to(@article.headword_presentation, pretty_article_path(@article))}

= render :partial => 'articles/info', :locals => { :article => @article }

= render :partial => "common/flash"

.grid_7.clearfix#edit_image
  = render partial: 'new', locals: { image: @image }

.grid_5
  %h2= t('articles.images.in_article')
  %aside.meta#article-images
    = render partial: 'show', :collection => @article.images, as: :image
    - if @article.images.empty?
      %p.no-images= t('articles.images.no_images')

  #license-info
    %h2= t('articles.images.license_info_header')
    %p= t('articles.images.license_info_html', url: "http://creativecommons.org/licenses/by-sa/3.0/no/")
