- header = capture do
  = render :partial => 'mobile/header', :locals => {:title => t('mime.mobile.title')}
  
- footer = capture do
  = render :partial => 'mobile/footer', :locals => {:footer => t('mime.mobile.footer')}
  
- content = capture do
  %div hepp

= render :partial => 'mobile/page', :locals => {:header => header, :footer => footer, :content => content}