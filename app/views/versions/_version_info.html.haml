%tr{:class => [(current ? 'current' : nil ), "version-number-#{version.version}"].compact.join(' ')}
  %td.version_number
    = version.version
  %td.version_date
    = l(version.updated_at)
  %td.author
    = version.authors_or_ip
